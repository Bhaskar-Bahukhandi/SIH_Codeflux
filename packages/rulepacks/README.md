# Versioned Rule Packs

This directory will contain machine-readable Legal Metrology checks only after each rule passes the legal source gate in `docs/legal/README.md`.

Every implemented rule pack must be versioned so a finalized inspection can retain the exact rule-set version used.

No rule should be added solely from memory, a blog, an AI-generated summary, or an unverified draft.
