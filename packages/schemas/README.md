# Shared Schemas

This package area is reserved for versioned contracts shared across applications/services where practical.

Likely future schemas include inspection, capture, declaration, finding, officer-review and synchronization payloads.

Do not create abstractions here until at least two real consumers need them.
