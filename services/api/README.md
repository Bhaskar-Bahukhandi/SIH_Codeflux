# Backend API

Planned technology: Python + FastAPI.

The API will eventually expose persisted inspection data and coordinate rule evaluation, reporting and synchronization boundaries.

Phase 0 does not implement compliance endpoints yet. Runtime/application code is introduced in the next roadmap phase so that data models and acceptance tests can be added together.
