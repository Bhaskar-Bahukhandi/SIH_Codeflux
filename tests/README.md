# Test Layout

Executable tests will be added with the code they validate.

Planned categories:

- `unit/`
- `integration/`
- `end_to_end/`
- `rule_regression/`

Core flows must fail visibly rather than silently returning fabricated successful output.
